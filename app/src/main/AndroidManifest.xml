<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pp.inzynierka.douslug">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".db.DBConnector"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.RegisterActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".db.DBTestActivity" />
        <activity android:name=".calendar.CalendarWeekActivity" />
        <activity android:name=".calendar.CalendarDayActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".calendar.CalendarMonthActivity" />
        <activity android:name=".VisitActivity" />
        <activity android:name=".ClientActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".AllVisitsActivity" />
        <activity android:name=".AllClientsActivity" />
        <activity android:name=".AllServicesActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".SettingsActivity" />
        <activity android:name=".FinanceActivity" />
    </application>

</manifest>